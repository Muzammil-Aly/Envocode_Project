/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx,ts,tsx}", // Looks inside the 'src' folder for JS, JSX, TS, and TSX files
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
